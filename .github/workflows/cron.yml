name: Call API every minute

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:   

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call API endpoint
        run: curl -X GET https://monex-eight.vercel.app/api/tasas
